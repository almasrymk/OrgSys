<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Administration.Domain/Administration.Domain.csproj" />
    <ProjectReference Include="../Administration.Contracts/Administration.Contracts.csproj" />
    <ProjectReference Include="../../../BuildingBlocks/OrgSys.EventBus/OrgSys.EventBus.csproj" />
    <!-- Domain.Resource.Translate (Role's permission-tree localization) depends on embedded
         .resx resources that still live in the legacy Domain project. Moving the resource
         files is a separate, higher-risk pass — see docs/modular-monolith-analysis.md §21.
         This is a deliberate, temporary exception to "modules don't depend on the monolith". -->
    <ProjectReference Include="../../../Domain/Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MediatR" Version="14.1.0" />
    <PackageReference Include="AutoMapper" Version="16.1.1" />
    <PackageReference Include="FluentValidation" Version="12.1.1" />
  </ItemGroup>

</Project>
