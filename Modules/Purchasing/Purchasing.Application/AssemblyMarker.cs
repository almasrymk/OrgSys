namespace Purchasing.Application;

/// <summary>Anchor type for assembly-scanning (MediatR registration, Architecture.Tests) while
/// this module has no business code of its own yet — see docs/module-ownership.md.</summary>
public sealed class AssemblyMarker;
