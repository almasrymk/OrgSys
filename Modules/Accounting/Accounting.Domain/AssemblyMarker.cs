namespace Accounting.Domain;

/// <summary>Non-functional type used only as a reflection anchor for this assembly (e.g. by Architecture.Tests and module registration). Delete once real entities give the assembly a natural anchor type.</summary>
public sealed class AssemblyMarker
{
    private AssemblyMarker() { }
}
